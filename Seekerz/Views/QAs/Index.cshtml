@model IEnumerable<Seekerz.Models.QA>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create" class="btn btn-success btn-sm">Create New Company</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Question)
            </th>
            
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Details" asp-route-id="@item.QAId"> @Html.DisplayFor(modelItem => item.Question)</a>
</td>
            
            @*<td>
                <a asp-action="Edit" asp-route-id="@item.QAId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QAId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QAId">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
